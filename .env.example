# Base URL for your internal OpenAI-compatible LLM server
INTERNAL_LLM_URL=

# Base URL for lab custom API (implement lab_adapter.py at the office)
LAB_LLM_URL=

# API key for default lab adapter (OpenAI-compatible httpx reference)
# LAB_OPENAI_API_KEY=ollama

# Optional: only for "make pip-cache" if your machine needs them (not used during "make build")
# HTTP_PROXY=
# PIP_INDEX_URL=
